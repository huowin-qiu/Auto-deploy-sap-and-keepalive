name: 自动部署Chrome-稳定优化版

on:
  workflow_dispatch:
    inputs:
      region:
        description: '选择部署区域'
        required: true
        default: 'US(free)'
        type: choice
        options:
          - SG(free)
          - US(free)
          - SAP-UAE(Dubai)
          - SAP-AU(Sydney)
          - SAP-JP(Tokyo)
      app_name:
        description: '应用名称'
        required: false
        default: ''

env:
  DOCKER_IMAGE: 'lscr.io/linuxserver/chrome:latest'
  # 调整为 3.5G，留出 0.5G 缓冲，防止 SAP 调度器直接崩溃
  MEMORY: '3584M'
  DISK: '2048M'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 安装 CF CLI
      run: |
        wget -q -O - https://packages.cloudfoundry.org/debian/cli.cloudfoundry.org.key | sudo apt-key add -
        echo "deb https://packages.cloudfoundry.org/debian stable main" | sudo tee /etc/apt/sources.list.d/cloudfoundry-cli.list
        sudo apt-get update && sudo apt-get install -y cf8-cli

    - name: 准备变量
      run: |
        RS=$(head /dev/urandom | tr -dc 'a-z' | head -c 4)
        if [ "${{ github.event.inputs.region }}" == "SG(free)" ]; then
          echo "CF_API=https://api.cf.ap21.hana.ondemand.com" >> $GITHUB_ENV
        else
          echo "CF_API=https://api.cf.us10-001.hana.ondemand.com" >> $GITHUB_ENV
        fi
        
        if [ -n "${{ github.event.inputs.app_name }}" ]; then
          echo "FINAL_APP_NAME=${{ github.event.inputs.app_name }}" >> $GITHUB_ENV
        else
          echo "FINAL_APP_NAME=chrome-opt-$RS" >> $GITHUB_ENV
        fi

    - name: 登录并清理
      run: |
        cf login -a ${{ env.CF_API }} -u "${{ secrets.EMAIL }}" -p "${{ secrets.PASSWORD }}"
        ORG=$(cf orgs | sed -n '4p')
        SPACE=$(cf spaces | sed -n '4p')
        cf target -o "$ORG" -s "$SPACE"
        cf delete ${{ env.FINAL_APP_NAME }} -f || true

    - name: 推送并设置超时
      run: |
        # 使用 -t 180 延长启动检查时间到 3 分钟，防止因为 Chrome 启动慢被杀掉
        cf push ${{ env.FINAL_APP_NAME }} --docker-image ${{ env.DOCKER_IMAGE }} -m ${{ env.MEMORY }} -k ${{ env.DISK }} --health-check-type port -t 180

    - name: 环境适配
      run: |
        cf set-env ${{ env.FINAL_APP_NAME }} VNC_PASSWORD "${{ secrets.VNC_PASSWORD || '123456' }}"
        cf set-env ${{ env.FINAL_APP_NAME }} TZ "Asia/Shanghai"
        # 针对 3.5G 内存优化 Chrome 参数
        cf set-env ${{ env.FINAL_APP_NAME }} CHROMIUM_FLAGS "--no-sandbox --disable-dev-shm-usage --disable-gpu --disable-software-rasterizer"
        
        cf restage ${{ env.FINAL_APP_NAME }}

    - name: 部署报告
      run: |
        ROUTE=$(cf app ${{ env.FINAL_APP_NAME }} | grep "routes:" | awk '{print $2}')
        echo "✅ 部署完成！访问地址: https://$ROUTE"
