name: 自动部署Chrome-预约明天版

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # 北京时间早上 8:00

env:
  # 换用 GitHub 托管的镜像源，绕过 Docker Hub 的限流
  DOCKER_IMAGE: 'ghcr.io/jlesage/chrome:latest'
  MEMORY: '2560M'
  DISK: '3072M'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 安装 CF CLI
      run: |
        wget -q -O - https://packages.cloudfoundry.org/debian/cli.cloudfoundry.org.key | sudo apt-key add -
        echo "deb https://packages.cloudfoundry.org/debian stable main" | sudo tee /etc/apt/sources.list.d/cloudfoundry-cli.list
        sudo apt-get update && sudo apt-get install -y cf8-cli

    - name: 登录并彻底清场
      run: |
        # 强制选择美国区运行
        cf login -a "https://api.cf.us10-001.hana.ondemand.com" -u "${{ secrets.EMAIL }}" -p "${{ secrets.PASSWORD }}"
        ORG=$(cf orgs | sed -n '4p')
        SPACE=$(cf spaces | sed -n '4p')
        cf target -o "$ORG" -s "$SPACE"
        # 暴力清空所有应用
        cf apps | awk 'NR>3 {print $1}' | xargs -I {} cf delete {} -f -r
        echo "FINAL_APP_NAME=chrome-8am-$(date +%s | tail -c 4)" >> $GITHUB_ENV

    - name: 推送应用 (Staging 关键步)
      run: |
        cf push ${{ env.FINAL_APP_NAME }} --docker-image ${{ env.DOCKER_IMAGE }} -m ${{ env.MEMORY }} -k ${{ env.DISK }} --no-start

    - name: 配置参数
      run: |
        cf set-health-check ${{ env.FINAL_APP_NAME }} port
        cf set-env ${{ env.FINAL_APP_NAME }} VNC_PASSWORD "${{ secrets.VNC_PASSWORD || '123456' }}"
        # 核心：将内部端口 5800 映射到系统环境，绕过只读 PORT 限制
        cf set-env ${{ env.FINAL_APP_NAME }} SECURE_CONNECTION 0
        cf set-env ${{ env.FINAL_APP_NAME }} CHROMIUM_FLAGS "--no-sandbox --disable-dev-shm-usage --user-data-dir=/tmp/c-data"

    - name: 启动
      run: |
        cf start ${{ env.FINAL_APP_NAME }}

    - name: 报告
      run: |
        ROUTE=$(cf app ${{ env.FINAL_APP_NAME }} | grep "routes:" | awk '{print $2}')
        echo "🚀 目标达成！地址: https://$ROUTE"
